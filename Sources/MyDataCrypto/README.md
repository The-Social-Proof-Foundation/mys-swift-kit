# MyDataCrypto

Native Swift MyData decrypt (BLS12-381 via blst + IBE/Shamir/DEM).

Part of `myso-swift-kit` — product `MyDataCrypto` from the root `Package.swift`.

Canonical protocol: TypeScript `@socialproof/mydata`. Milestone 0 fixtures live in `Fixtures/` and `Tests/MyDataCryptoTests/Fixtures/`.
